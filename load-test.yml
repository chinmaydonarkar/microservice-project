config:
  target: "http://localhost"
  phases:
    - duration: 60   # Run for 1 minute
      arrivalRate: 300  # 50 requests per second

scenarios:
  - flow:
      - get:
          url: "/auth/login"
